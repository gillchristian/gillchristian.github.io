blockquote
  | I would like to be able to always divide the things up into as many pieces as I can, each of which I understand separately.
  | I would like to understand the way of adding things up, independently of what it is I’m adding up.
  br
  b
    i Gerald Sussman
  | .

h1 Talks at meetups & conferences

.flex-grid
  - each talk in public._data.talks
    .flex-item
      .card
        .card-header(style="background-color: #{talk.color}")
          strong= talk.title
        .card-content
          .inner
            a(href="#{talk.eventUrl}", target="_blank", rel="noopener noreferrer")= talk.event
          .inner
            i.fa.fa-clock-o
            | #{' ' + talk.date}
          .inner= talk.description
          .inner
            - each link, index in talk.links
              a(href="#{link.url}", target="_blank", rel="noopener noreferrer")= link.label
              if index < (talk.links.length - 1)
                | #{' | '}
